// Expose APIs to the renderer process safely here if needed
// Example: const { contextBridge, ipcRenderer } = require('electron');
window.addEventListener("DOMContentLoaded", () => {
  console.log("Preload script loaded");
});
